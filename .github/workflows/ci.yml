name: CI

permissions:
  contents: read
  checks: write
  pull-requests: write

on:
  push:
    branches: [ main, master ]
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '8.0.x'

      - name: Restore
        run: dotnet restore

      - name: Build
        run: dotnet build --configuration Release --no-restore

      # 🔥 DEMO API'Yİ AYAĞA KALDIRMACA
      - name: Start Demo API
        run: |
          cd ApiTesting.DemoApi
          dotnet run --urls=http://localhost:5052 &
          echo "Demo API started"

      # ⏳ API AYAĞA KALKANA KADAR BEKLEMECE
      - name: Wait for Demo API
        run: |
          for i in {1..20}; do
            if curl -s http://localhost:5052/health; then
              echo "API is up"
              exit 0
            fi
            echo "Waiting for API..."
            sleep 2
          done
          echo "API did not start in time"
          exit 1

      # ✅ TESTLER
      - name: Test (with coverage)
        run: |
          dotnet test ApiTesting.Specs/ApiTesting.Specs.csproj \
            --configuration Release \
            --no-build \
            --logger "trx;LogFileName=test_results.trx" \
            --collect:"XPlat Code Coverage"

      - name: Upload test results
        uses: actions/upload-artifact@v4
        with:
          name: test-results
          path: '**/TestResults/*.trx'

      - name: Publish test results to Checks
        uses: dorny/test-reporter@v1
        if: always()
        with:
          name: dotnet-tests
          path: '**/TestResults/*.trx'
          reporter: dotnet-trx
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Upload coverage
        uses: actions/upload-artifact@v4
        with:
          name: coverage
          path: '**/TestResults/**/coverage.cobertura.xml'

      - name: Upload coverage to Codecov
        uses: codecov/codecov-action@v4
        with:
          files: '**/TestResults/**/coverage.cobertura.xml'
          fail_ci_if_error: false
